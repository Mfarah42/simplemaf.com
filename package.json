{
  "name": "simplemaf-site",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "description": "SimpleMAF creator site. TypeScript source, single static HTML artifact.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "tsc --noEmit && vitest run"
  },
  "devDependencies": {
    "jsdom": "^25",
    "typescript": "^5.5.0",
    "vite": "^8.2.0",
    "vite-plugin-singlefile": "^2.0.0",
    "vitest": "^4.1.10"
  }
}
